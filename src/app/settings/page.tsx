import { SectionPlaceholder } from "@/components/section-placeholder";

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings"
      description="Settings route for account preferences and aircraft configuration controls."
    />
  );
}
